{
  "name": "cc-check-vercel",
  "version": "1.0.0",
  "private": true,
  "description": "Simple serverless card checker API",
  "main": "api/check.js",
  "engines": {
    "node": ">=18.x"
  },
  "scripts": {
    "dev": "echo 'No local dev – test via curl'"
  }
}
